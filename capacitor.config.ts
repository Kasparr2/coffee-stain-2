import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Coffee Stain2',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
